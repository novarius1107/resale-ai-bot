services:
  resale-bot:
    build: .
    container_name: resale-ai-store-bot
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "8000:8000"
      - "8501:8501"
    volumes:
      - ./data:/app/data
      - ./resale_bot.db:/app/resale_bot.db
